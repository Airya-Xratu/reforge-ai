# Example: Flutter App Project Fingerprint
# Generated by Reforge project-archaeologist

project:
  name: "taskmaster"
  type: mobile
  framework: flutter
  language: dart
  framework_version: "3.22.0"
  language_version: "3.4.0"

architecture:
  pattern: feature_first
  state_management: flutter_bloc
  dependency_injection: get_it
  navigation:
    package: go_router
    pattern: declarative

backend:
  type: rest
  base_url: "https://api.taskmaster.app/v2"
  auth_type: jwt

ui:
  design_system: custom
  theme: material3
  dark_mode: supported
  responsive: false

testing:
  unit: true
  widget: true
  integration: true
  e2e: false
  mocking_library: mocktail

platforms:
  android: true
  ios: true
  web: false
  desktop: false

dependencies:
  total: 28
  core:
    - flutter_bloc
    - go_router
    - get_it
    - injectable
    - dio
    - freezed_annotation
    - json_annotation
    - hive
  dev:
    - build_runner
    - freezed
    - json_serializable
    - mocktail
    - bloc_test
    - integration_test

ci_cd:
  provider: codemagic
  stages: [build, test, deploy]

confidence: HIGH
notes: >
  Well-structured feature-first Flutter app. Clean architecture boundaries.
  Good test coverage in core features. Some tech debt in older features (pre-BLoC migration).
