# Agent Handoff Document

> Generated by Reforge v2.0. All context lives in `reforge/project-model.yaml`.
> This document summarizes key findings for quick consumption.

---

## QUICK REFERENCE

| Field | Value |
|---|---|
| Project | `project_model.fingerprint.project.name` |
| Framework | `project_model.fingerprint.project.framework` |
| Language | `project_model.fingerprint.project.language` |
| Architecture | `project_model.architecture.pattern` |
| State Mgmt | `project_model.architecture.state_management.approach` |
| Navigation | `project_model.fingerprint.navigation.package` |
| Design System | `project_model.design_system` quality assessment |

---

## PRODUCT SNAPSHOT

**Purpose:** `project_model.product.purpose`

**Core Features:**
List from `project_model.features` where category = core.

**Product DNA:**
```yaml
# Copy from project_model.product_dna
```

---

## KEY SCREENS

Top 5-8 screens from `project_model.screens` (sorted by entry_points count).

For each: id, name, purpose, type, key components, states.

---

## CRITICAL STATE MACHINES

From `project_model.state_machines` — screens with most complex state handling.

---

## ARCHITECTURE SUMMARY

```yaml
# Copy key fields from project_model.architecture
```

---

## DESIGN SYSTEM ESSENTIALS

```yaml
# Copy from project_model.design_system — colors, typography, spacing
```

---

## IMPLEMENTATION PLAN

Reference `project_model.decision_log` for key decisions.

Phase plan from implementation-planner output.

---

## FILE PLAN

From implementation-planner: files to create, modify, delete.

---

## HEALTH SCORES

From `project_model.health` — dimension scores and critical issues.

---

## OPEN QUESTIONS

From `project_model.decision_log` where confidence = LOW or overridden.

---

## HOW TO USE THIS

1. Read `reforge/project-model.yaml` for full data
2. This document is a summary — the YAML has all details
3. Every finding has evidence (source file + line)
4. Check `project_model.overrides` for user preferences
5. Check `project_model.decision_log` for reasoning behind recommendations
